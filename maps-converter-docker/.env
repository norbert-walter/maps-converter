###############################################
# Basis Configuration for Docker Compose
###############################################

# Port on the host → <PORT>:8080 in the container
PORT=8080

# Environment:
#   production  = Gunicorn (Dockerfile CMD)
#   development = Flask Dev Server (Compose Override)
FLASK_ENV=production

# Time zone to be used in the container
# -> /usr/share/zoneinfo/<TZ>
TZ=Europe/Berlin

###############################################
# Gunicorn / Performance Settings
###############################################

# Number of workers [2,4] (parallel processes)
WORKERS=4

# Threads per worker (simultaneous requests per worker)
THREADS=4

# Gunicorn Timeout (if a request takes longer)
TIMEOUT=60

###############################################
# Paths (mounted as volumes)
###############################################

TILE_CACHE_DIR=./tile_cache
LOG_DIR=./logs
###############################################

